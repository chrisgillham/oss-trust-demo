# Trusted publisher allowlist for oss-trust-demo
# Maps the demo app's dependencies to their canonical GitHub repos.

PyPI:
  "requests": "psf/requests"
  "rich": "Textualize/rich"
  "click": "pallets/click"
  "oss-trust-framework": "chrisgillham/oss-trust-framework"

npm: {}
Cargo: {}

require_attestation:
  PyPI:
    - "requests"
